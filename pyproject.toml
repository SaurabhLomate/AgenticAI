[project]
name = "agenticai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "unstructured>=0.18.32",
]

[tool.uv.workspace]
members = [
    "LangChain",
]
